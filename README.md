# Healthcare — Early Sepsis (demo)
Purpose: tabular classification demo with AUROC.  
Run: `python src/main.py`  
Next: add real features (vitals/labs), XGBoost + SHAP, and calibration; document clinical thresholds.
